let youtuber = {
  list: {
    business: {
      youtuber: [{ name: "しまぶー", age: 20, teachprogramming() {} }],
      teach() {},
    },
    entertaiment: {
      youtuber: [{ name: "ひかきん" }, { name: "はじめしゃちょー" }],
      makesmile() {},
    },
  },
  plan() {},
  uploadvideo() {},
};

const foo = document.getElementById("foo");

const foowidth = foo.offsetWidth;
const fooHeight = foo.offsetHeight;

const fooArea = foowidth * foowidth;

const foo = 100,
  foo2 = 100,
  foo3 = 1000;
let bar = 200;
var baz = 300;

const constnumber = 100;
const constnumber = 200;
